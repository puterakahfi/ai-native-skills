name: Sync Generated Capability Metadata

on:
  workflow_dispatch:
  push:
    branches:
      - "feat/**"
      - "fix/**"
      - "test/**"
      - "chore/**"
      - "epic/**"
    paths:
      - "skills/**"
      - "README.md"
      - "docs/skills.md"
      - "scripts/run-eval.sh"
      - "scripts/verify-capability-inventory.py"
      - "scripts/inventory-contract-coverage.py"
      - ".github/workflows/sync-generated-capability-metadata.yml"

permissions:
  contents: write

concurrency:
  group: sync-generated-capability-metadata-${{ github.ref }}
  cancel-in-progress: false

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout executable repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Resolve pinned Core revision
        id: core-ref
        shell: bash
        run: |
          set -euo pipefail
          core_ref="$(sed -n 's/^CORE_REF="\(.*\)"/\1/p' scripts/run-eval.sh)"
          test -n "$core_ref"
          echo "ref=$core_ref" >> "$GITHUB_OUTPUT"
      - name: Checkout pinned canonical Core
        uses: actions/checkout@v4
        with:
          repository: puterakahfi/ai-native-core
          ref: ${{ steps.core-ref.outputs.ref }}
          path: .deps/ai-native-core
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install metadata dependencies
        run: python -m pip install --disable-pip-version-check PyYAML
      - name: Synchronize documented inventory counts
        shell: python
        run: |
          import importlib.util
          import re
          from pathlib import Path
          script = Path("scripts/verify-capability-inventory.py")
          spec = importlib.util.spec_from_file_location("capability_inventory", script)
          module = importlib.util.module_from_spec(spec)
          spec.loader.exec_module(module)
          counts = module.derive_inventory()["counts"]
          readme = Path("README.md")
          readme_text = readme.read_text(encoding="utf-8")
          readme_text, replaced = re.subn(r"\*\*\d+ skills · \d+ workflows · \d+ meta-skills\*\*", f"**{counts['skill']} skills · {counts['workflow']} workflows · {counts['meta-skill']} meta-skills**", readme_text, count=1)
          if replaced != 1: raise SystemExit("README inventory summary was not updated exactly once")
          readme.write_text(readme_text, encoding="utf-8")
          taxonomy = Path("docs/skills.md")
          taxonomy_text = taxonomy.read_text(encoding="utf-8")
          replacements = {r"^- `skill`: \d+$": f"- `skill`: {counts['skill']}", r"^- `workflow`: \d+$": f"- `workflow`: {counts['workflow']}", r"^- `meta-skill`: \d+$": f"- `meta-skill`: {counts['meta-skill']}", r"^- Total executable skills: \d+$": f"- Total executable skills: {counts['total']}"}
          for pattern, replacement in replacements.items():
              taxonomy_text, replaced = re.subn(pattern, replacement, taxonomy_text, count=1, flags=re.MULTILINE)
              if replaced != 1: raise SystemExit(f"taxonomy inventory line was not updated exactly once: {pattern}")
          taxonomy.write_text(taxonomy_text, encoding="utf-8")
      - name: Regenerate capability inventory
        run: python scripts/verify-capability-inventory.py --write-snapshot
      - name: Regenerate contract coverage evidence
        run: >-
          python scripts/inventory-contract-coverage.py
          --root .
          --core-root .deps/ai-native-core
      - name: Commit generated metadata
        shell: bash
        run: |
          set -euo pipefail
          git add README.md docs/skills.md docs/capability-inventory.json docs/contract-coverage-discovery.yaml
          if git diff --cached --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore(metadata): sync generated capability evidence"
          git push origin "HEAD:${GITHUB_REF_NAME}"
