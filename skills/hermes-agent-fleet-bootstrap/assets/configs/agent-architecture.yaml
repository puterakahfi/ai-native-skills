# agent-architecture profile config
model:
  provider: copilot
  default: gpt-5.5
agent:
  max_turns: 60
  verbose: false
  reasoning_effort: high
terminal:
  timeout: 180
compression:
  enabled: true
  threshold: 0.5
  target_ratio: 0.2
