contract_exemption:
  schema_version: 1.0.0
  artifact:
    id: hermes-auto-routing-planner
    path: skills/hermes-auto-routing-planner/SKILL.md
    type: skill
  classification: provider_specific
  owner: puterakahfi
  reason: >
    Auto-routing plan schema (task-routing-plan.schema.yaml) is a Hermes-specific JSON schema, not a cross-runtime skill contract. The planner produces Hermes-native plan artifacts with no verified cross-runtime semantics yet.
  scope:
    - provider_specific_execution
    - runtime_schema_reference
  review_date: "2027-01-31"
  evidence_refs:
    - kind: github_issue
      ref: puterakahfi/ai-native-skills#304
    - kind: executable_source
      ref: skills/hermes-auto-routing-planner/SKILL.md
  revisit:
    blocking_issue: null
    triggers:
      - A second runtime adapter implements equivalent plan-based routing.
      - task-routing-plan contract is promoted to ai-native-core.
  prohibited_claims:
    - core_contract_conformance
    - behavioral_verification
    - runtime_acceptance
    - product_acceptance
    - approval_or_authorization
  status: active
