# Negative fixture — orchestrator handled a code-writing request without delegating.
# Violates: §2.1 (implementation is NOT in the self-handle allow-list).
# Expected failure: orchestrator_action_receipt.category invalid for input.
# Schema-level failure: category value 'implementation' is not in enum.

plan:
  schema_version: "1.0"
  plan_id: plan-selfhandle-out-of-scope-001
  created_at: "2026-07-31T14:00:00Z"
  origin:
    channel: desktop
    session_id: sess-shortcut-99
  orchestrator_action:
    kind: self_handled
    # NOTE: implementation is NOT in enum → schema rejects
    self_handled_category: implementation
  primary_workflow: orchestrator-self-handled
  status: blocked

orchestrator_action_receipt:
  schema_version: "1.0"
  receipt_id: orchact-shortcut-001
  plan_id: plan-selfhandle-out-of-scope-001
  category: implementation
  rationale: "Orchestrator wrote a React component itself instead of delegating to agent-frontend."
  input_summary: "User: bikin komponen navbar"
  output_summary: "Orchestrator emitted a .tsx file"
  started_at: "2026-07-31T14:00:00Z"
  completed_at: "2026-07-31T14:15:00Z"
  status: executed

expected_failure:
  schema: orchestrator-action-receipt
  reason: "category 'implementation' is not in enum §2.1"
